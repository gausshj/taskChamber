coverage:
  status:
    # Gate the total project coverage so main cannot silently regress; auto
    # tracks the base commit and the threshold absorbs small fluctuations.
    project:
      default:
        target: auto
        threshold: 1%
    # Gate the diff coverage of each pull request.
    patch:
      default:
        target: 80%
